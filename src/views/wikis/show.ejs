<!DOCTYPE html>
<html>

<head>
  <% include ../static/partials/head.ejs %>
</head>

<body>
  <% include ../static/partials/navbar.ejs %>

    <main class="container">

      <% include ../static/partials/messages.ejs %>

        <h1>
          <%= wiki.title %>
        </h1>
        <hr/>

        <% if (currentUser && (currentUser.isAdmin() || (currentUser.isPremium() && currentUser.id === wiki.userId)) ) { %>
          <a class="btn btn-info" href="/wikis/<%= wiki.id %>/collaborators/edit">Edit Collaborators</a>
        <% } %>


        <%# if (currentUser && (currentUser.id === wiki.userId || currentUser.isAdmin()))  { %>
          <a href="/wikis/<%= wiki.id %>/edit" class="btn btn-warning text-white">Edit</a>
        <%# } %>
        <% if (currentUser && (currentUser.isAdmin() || currentUser.id === wiki.userId )) { %>
          <form style="display:initial" action="/wikis/<%= wiki.id %>/destroy" method="post">
            <button type="submit" class="btn btn-danger text-white">Delete</button>
          </form>
        <% } %>

    <hr/>

              <%- wiki.body %>
            </p>

    </main>

    <% include ../static/partials/baseScripts.ejs %>

</body>

</html>